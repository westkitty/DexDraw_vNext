export function installCanvasZoom() {
  // Temporarily disabled while the canvas shell visibility regression is fixed.
  // Keep the export so main.tsx remains stable until the zoom feature is rebuilt
  // inside React instead of through DOM mutation.
}
